This library contains 3 files:

- `index.html`: a template to test the library.
- `searchLibrary.js`: the JavaScript library to search and filter data
- `data.json`: the local data to search for.
To test the  library, run the following command from the shell:

python -m http.server

Open a browser to http://localhost:8000/ and check the interaction with index.html file

