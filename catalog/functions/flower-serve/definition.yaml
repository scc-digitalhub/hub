
kind: flower-server
metadata:
  name: Flower Serve (flower-server)
  version: 1.0.0
  description: This function serves as a demonstration of the Flower Supernode server functionality. It builds the corresponding Docker image, and activate server node containers with the necessary dependencies and specific configurations tailored for each node.
  labels:
    - "license:apache-2.0"
    - "platform:0.14"
    - "type:service"
    # - "framework:flower"
name: flower-serve
python_version: PYTHON3_10
spec:
  requirements:
    - pandas==2.2.3
    - flwr-datasets[vision]==0.5.0
---
kind: flower-server+deploy:run
spec:
